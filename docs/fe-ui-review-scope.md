# FE UI Review Scope

Backend/API is out of scope.

P0: consolidate screen-local styling into shared UI primitives; select one canonical recording interaction and hide A/B/C from normal users.
P1: make VoiceBubble the visual source of truth; unify typography, spacing, feedback, accessibility and motion.
P2: polish responsive layouts and micro-interactions.

Sequence: interaction → VoiceBubble → shared UI → typography/spacing → feedback → accessibility/motion → responsive → visual QA.

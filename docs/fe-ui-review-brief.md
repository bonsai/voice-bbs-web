# FE UI Review Brief

Backend/API is out of scope.

P0: screen-local CSS/inline styling should move to shared Design System primitives; select one canonical recording interaction and hide A/B/C from normal users.
P1: VoiceBubble should be the single visual source for waveform, category, owner, playback, loading and error states; unify typography, spacing, feedback, accessibility and motion.
P2: polish responsive composition and micro-interactions.

Order: interaction model → VoiceBubble → shared UI → typography/spacing → feedback → accessibility/motion → responsive → visual QA.
